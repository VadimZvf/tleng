package main

import (
	"testing"
)

func TestGetKeySymbol(t *testing.T) {
	// symbolToken, isFoundSymbol := GetKeySymbol("sadad", 0)
	// if isFoundSymbol == false {
	// 	t.Errorf("Sum was incorrect, got: %d, want: %d.", symbolToken, 10)
	// }
}
